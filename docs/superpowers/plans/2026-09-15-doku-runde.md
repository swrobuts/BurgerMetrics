# Doku-Runde nach Phase 7 — Implementierungsplan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Die technische Dokumentation (`docs/`), die READMEs und die Spec beschreiben den Stand nach Phase 7 — Rezensionen als dritte Faktentabelle, 15 CSV-Dateien, Notebooks, Dash, MCP und BM-Lab im Repository-Baum, Prüfberichte vom 15.09.2026 — ohne neue Behauptungen.

**Architecture:** Ein Task, nur Textänderungen an bestehenden Dokumenten in ihrer eigenen Stimme; jede Zahl aus dem Repository (verifiziert unten); ein Review; ein PR.

**Tech Stack:** Markdown, Mermaid (Prüfung per MCP oder mermaid-cli), Python-Einzeiler für Link- und Anführungszeichenprüfung.

**Spec:** Nachtrag zu `docs/superpowers/specs/2026-09-12-bm-analyse-lernumgebung-design.md` §10 („Verknüpfungen und Dokumentation“); Auftrag von Robert am 15.09.2026: „mach die Doku-Runde“ (Baum in `docs/README.md`, ASCII-Schlusszeichen in der Spec) — erweitert um die Stellen, die beim Survey als veraltet auffielen.

## Global Constraints

- **Repository und Zweig:** `/Users/robert/Library/CloudStorage/OneDrive-Persönlich/Vorlesungen/Datenbasierte Fallstudien/BurgerMetrics/BurgerMetrics_Website`, Branch `bm-analyse` (steht auf `main` = 2e2169a). Absolute Pfade; nie in einem Worktree unter `BurgerMetrics/.claude/worktrees/` arbeiten — leitet das Edit-Werkzeug dorthin um, mit Python direkt im Repository schreiben und per `grep` prüfen. Commit `git -c core.fileMode=false add …`, deutsch, Trailer `Co-Authored-By: <Modell> <noreply@anthropic.com>`; PR ohne Merge.
- **Unversionierte Dateien im Arbeitsbaum nicht anfassen und nicht committen:** `docs/validierung-colab-dash-2026-09-15.md`, `notebooks/tests/test_externe_daten.py` und die fünf `*-HP-Desktop*`-Kopien (Roberts Stand vom Windows-Rechner, Entscheidung offen).
- **Sprache:** Deutsch, echte Umlaute, sachlich, „…“-Anführungszeichen in Prosa, keine Ausrufezeichen; jedes Dokument in seiner Stimme fortschreiben (docs/: nummerierte Kapitel, `---`, „Weiter:“-Zeile; README: knapp; Mermaid-Knoten kurz). Nichts umschreiben, was nicht in der Liste steht.
- **Verifizierte Fakten (15.09.2026):** `dataset/` enthält 15 CSV-Dateien in Git LFS: zehn Dimensionen, drei Fakten (`fact_orders`, `fact_order_items`, `fact_reviews`), `obt_orders.csv`, `fact_reviews_roh.csv`; Galaxy-Dateien (13) rund 137 MB, `obt_orders.csv` 176 MB, gesamt rund 315 MB. `db/lade_csv.py` lädt dreizehn Tabellen (zehn Dimensionen, drei Fakten) — dieser Satz in `db/README.md:23` bleibt richtig. Schema `burgermetrics`: 14 Tabellen (zehn Dimensionen, drei Fakten, `obt_orders`), 38 materialisierte Sichten; das Dashboard lädt 33 davon (`web/js/reihen.js`) — „33 Sichten“ in `README.md`/`db/README.md` bleibt. `fact_reviews`: 10.000 Zeilen, Grain eine Rezension, `source = 'simulation'`, Fremdschlüssel auf `dim_date`, `dim_customer`, `dim_product`, `dim_branch`, `fact_orders`. Generator `dataset/generate_reviews.py` liegt im Repository (Sterne aus Zufriedenheit und Bestelldauer, Bausteintexte), die Texte wurden anschließend in Claude Code geglättet (`fact_reviews_roh.csv` → `fact_reviews.csv`); die Generierung der Bestellungen bleibt außerhalb des Repositorys. Der Bericht liest die Semantikschicht zur Laufzeit (E5 zurückgenommen, docs/05 §5.3). Committete Prüfberichte unter `docs/`: `validierung-dashboard-2026-04-10.md` (42 Werte), `-2026-04-12.md` (185 Werte), `-2026-08-26.md` (72 Karten), `validierung-notebooks-2026-09-15.md` (alle neun Notebooks), `validierung-nur-lesezugang-2026-09-15.md` (Rechte der Rolle), `validierung-repo-2026-09-15.md` (Repository-Prüfung) — sechs. Repository-Baum: `web/` (index, pos, shop, dashboard, abgleich, `js/`, `tests/`, `lab/`), `dataset/`, `db/` (`aufbau/`, `betrieb/`, `tests/`, `lade_csv.py`, `materialisieren.py`, `skript_ausfuehren.py`), `notebooks/` (00–08, `quellen/`, `daten_extern/`, `export/`, `tests/`), `dash/`, `mcp/`, `docs/`, `.github/workflows/static.yml`, `.gitattributes`, `.gitignore`, `.env.example`; Pages liefert `web/` (rund 20 MB davon `web/lab/`, PGlite 19 MB).

---

### Task 1: Dokumentation auf den Stand nach Phase 7

**Files:**
- Modify: `docs/README.md`, `docs/03-etl.md`, `docs/04-validierung.md`, `docs/06-betrieb.md`, `docs/07-lehrbezug.md`, `docs/08-entscheidungen.md`, `dataset/README.md`, `docs/superpowers/specs/2026-09-12-bm-analyse-lernumgebung-design.md`, `web/lab/lab-01-zugang.html`

- [ ] **Step 1: `docs/README.md`** — Abschnitt „Aufbau des Repositorys“: den Baum durch den vollständigen Baum aus den Global Constraints ersetzen (eine Zeile je Verzeichnis mit Kurzinhalt, `dataset/` mit „15 CSV-Dateien (Git LFS) · generate_obt.py · generate_reviews.py · load_duckdb.py · verify_readme.py · wawi_mini.sql · burgermetrics_mini.sql · wawi_zu_analytisch.sql · README.md · uebungsblatt.md/.pdf · erp_datenmodell.excalidraw“, `web/` mit `lab/`). Tabelle „Kennzahlen des Datenbestands“: Zeile `| Rezensionen | 10.000 |` nach „Bestellpositionen“. Abschnitt „Prüfberichte“: Einleitungssatz „zweimal“ → auf die sechs Berichte anpassen und die drei Berichte vom 15.09.2026 als weitere Listenpunkte mit je einem Halbsatz (Notebooks: alle neun ausgeführt; Nur-Lesezugang: Rechte der Rolle `studi_daba`; Repository: Prüfung des Bestands) — Wortlaut aus den Kopfzeilen der Berichte.
- [ ] **Step 2: `docs/03-etl.md`** — §3.1 Diagramm: `STAR` → „Galaxy-Schema<br/>13 CSV-Dateien · rund 137 MB<br/>3 Fakten + 10 Dimensionen“, neuer Knoten `REV["generate_reviews.py<br/>10.000 Rezensionen · geglättet"]` mit Kante `REV --> STAR` (Stil wie `VER`), `DASH` → „dashboard.html<br/>liest 33 Sichten zur Laufzeit“, `VAL` → „docs/validierung-*.md<br/>sechs Prüfberichte“; Diagramm mit dem Mermaid-MCP prüfen. Text nach dem Diagramm und §3.2: „die Generierung selbst nicht Teil des Repositorys“ → „die Generierung der Bestellungen nicht Teil des Repositorys; die Rezensionen erzeugt seit September 2026 `dataset/generate_reviews.py` im Repository, die Texte wurden anschließend geglättet (`fact_reviews_roh.csv` → `fact_reviews.csv`, [Entscheidung E10](08-entscheidungen.md#e10))“ — an beiden Stellen sinngemäß, §3.2-Titel „Warum die Generierung der Bestellungen nicht im Repository liegt“. §3.3: „ist die einzige Transformation im Repository“ → „ist die Transformation für die One Big Table“. §3.5: „311 MB“ → „rund 315 MB (15 Dateien)“ an beiden Stellen (Zeile ~143 und ~161, falls dort).
- [ ] **Step 3: `docs/04-validierung.md`** — §4.3 nach dem Absatz zum 12. April den Bericht vom 26.08.2026 (72 Karten und Management Summary, fünf Korrekturen, Abschluss — Wortlaut aus `docs/README.md`) und einen kurzen Abschnitt „### Prüfungen vom 15. September 2026“ mit den drei Berichten (je ein Satz, Verweis) ergänzen.
- [ ] **Step 4: `docs/06-betrieb.md`** — §6.1 Baum wie in Step 1 (Baumschreibweise mit `├──` beibehalten; `docs/` „diese Dokumentation · sechs Prüfberichte“); der Satz „Die Trennung folgt der Frage, wer was braucht“ um `db/`, `notebooks/`, `dash/` (werden ausgeführt) und `mcp/` (Betreiberzugang) erweitern. §6.2: nach dem Diagramm ein Satz, dass das Artefakt seit Phase 6 auch `web/lab/` mit der PGlite-Laufzeit enthält (rund 20 MB, davon 19 MB PGlite; kein Build-Schritt). „311 MB“ in §6 → „rund 315 MB“.
- [ ] **Step 5: `docs/07-lehrbezug.md:13`** — „das analytische Modell (12 Tabellen)“ → „das analytische Modell (14 Tabellen: zehn Dimensionen, drei Fakten, eine One Big Table)“.
- [ ] **Step 6: `docs/08-entscheidungen.md`** — E2-Tabelle, Zeile „Zwei Faktentabellen, geteilte Dimensionen — gewählt“: Zusatz „(seit September 2026 drei, [E10](#e10))“ in der Bewertungsspalte.
- [ ] **Step 7: `dataset/README.md:21`** — „Das Modell hat **zwei Faktentabellen auf unterschiedlicher Granularität**, die sich Dimensionen teilen.“ → „Das Modell hat **zwei Verkaufs-Faktentabellen auf unterschiedlicher Granularität**, die sich Dimensionen teilen, und seit September 2026 eine dritte, `fact_reviews` (unten).“ Danach `python3 dataset/verify_readme.py` ausführen (Exit 0 — prüft Zahlen, nicht Prosa; falls das Skript LFS-Daten braucht und fehlschlägt, den Grund berichten).
- [ ] **Step 8: Spec** — die 14 deutschen Anführungszeichen, die mit ASCII `"` schließen (Muster `„…"` ohne `“`), auf `“` setzen; Python-Einzeiler vorher/nachher: Zahl der `„` = Zahl der `“` je Zeile für die betroffenen Zeilen (166, 211, 238, 252, 253, 257, 259, 298, 407, 429, 432 ×2, 433, 434, 496 — Zeilennummern Stand 2e2169a). Keine sonstige Änderung an der Spec.
- [ ] **Step 9: `web/lab/lab-01-zugang.html`** — vor dem `.vorlagen`-Block (Zeile ~144, nach der DataGrip-Nachbildung) ein Vorsatz-Absatz: „Beide Vorlagen enthalten die fünf Angaben mit Erklärung — zum Kopieren in psql beziehungsweise in den DBeaver-Dialog.“ (prüfen, was die zwei Karten tatsächlich enthalten, und den Satz daran anpassen).
- [ ] **Step 10: Prüfen** — Link-Skript über alle geänderten Markdown-Dateien (relative `](…)`-Ziele existieren); Mermaid-Prüfung §3.1; `node web/lab/tools/verify.mjs 2>/dev/null | tail -1` (782/0); `grep -n "13 CSV\|12 Tabellen\|zwei Prüfberichte\|einzige Transformation\|fest eingetragene" docs/*.md` → keine Treffer außer historischen Aussagen in Entscheidungsjournal-Einträgen (E5, E8), die als Geschichte bleiben; Anführungszeichen-Einzeiler über die Spec.
- [ ] **Step 11: Commit und Push** — `docs: Doku-Runde nach Phase 7 — Repository-Baum, dritte Faktentabelle, Prüfberichte, Spec-Typografie`; `git push origin bm-analyse`; kein PR (öffnet der Controller nach dem Review).

---

## Selbstprüfung des Plans

**Abdeckung:** Roberts zwei Punkte (Baum in `docs/README.md`, Spec-Typografie) — Step 1, 8; Survey-Funde: `docs/06` Baum und LFS-Zahl (4), `docs/03` Generierung/OBT/Dashboard/LFS (2), `docs/04` fehlende Berichte (3), `docs/07` Tabellenzahl (5), `docs/08` E2 (6), `dataset/README` Faktentabellen (7), Lab-01-Vorsatz aus dem Endreview Phase 7 (9). **Nicht geändert (bewusst):** „33 Sichten“ (stimmt für das Dashboard), „dreizehn CSV-Dateien“ in `db/README.md:23` (stimmt für `lade_csv.py`), E5/E8 als Geschichte, `README.md` (seit Phase 7 aktuell).
