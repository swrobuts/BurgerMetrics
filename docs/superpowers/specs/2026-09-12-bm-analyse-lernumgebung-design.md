# BurgerMetrics — Analyse-Deck, Notebooks, Rezensionen und BM-Lab

Entwurf vom 12.09.2026, mit Robert abgestimmt; am selben Tag auf ein Repo umgestellt (BM-Lab unter `web/lab/`). Dieses Dokument ist die Vorlage für die
Umsetzung in einer neuen Sitzung im Repo `BurgerMetrics_Website`; der Implementierungsplan
entsteht daraus je Phase mit dem Skill `writing-plans`.

## 1 Ziel

Studierende des Moduls *Datenbasierte Fallstudien* sollen mit dem BurgerMetrics-Bestand
eigenständig analysieren können: sich an der Datenbank anmelden, Daten für Power BI, Tableau
und Python beschaffen, ein analytisches Datenmodell schrittweise aufbauen, Data-Mining-Verfahren
anwenden und externe Daten anbinden. Dafür entstehen vier Dinge:

1. ein Foliendeck `BurgerMetrics_3_Analyse.pptx` als dritter Teil der bestehenden Reihe,
2. neun Jupyter-Notebooks und eine Dash-App im BM-Repo,
3. eine Erweiterung des Bestands um **Rezensionen** (Datenbank, Generator, Shop-Feature),
4. eine interaktive Lernumgebung **BM-Lab** unter `web/lab/` im selben Repo, auf GitHub Pages.

**Produktionsbereite Übergabe.** Robert führt vor oder in einer Lehrveranstaltung keine
Skripte aus und arbeitet dort oft nicht am eigenen Rechner. Deshalb gilt für jede Phase: Was
gebaut wird, wird von der Sitzung selbst in Betrieb genommen — Aufbauskripte auf der
Instanz eingespielt (über den MCP-Server `burgermetrics-db`, `ausfuehren()`), CSV geladen,
Sichten materialisiert, Seiten veröffentlicht, Notebooks ausgeführt, Verweise gesetzt. Eine
Phase ist erst abgeschlossen, wenn der Stand ohne weiteren Handgriff nutzbar ist.

Zielgruppe: Bachelor Business Analytics ohne tiefe Programmiererfahrung. Alles Material ist
sachlich, nur deutsch, englische Fachbegriffe bleiben englisch, echte Umlaute, Code für
Anfänger (kurze Funktionen, deutsche Namen, ein Kommentar je Funktion, keine Tricks).

## 2 Rahmen

### 2.1 Orte

| Was | Wo |
|---|---|
| BM-Repo | `Vorlesungen/Datenbasierte Fallstudien/BurgerMetrics/BurgerMetrics_Website` — GitHub `swrobuts/BurgerMetrics`, Pages `swrobuts.github.io/BurgerMetrics/` (nur `web/`) |
| Arbeitszweig | `bm-analyse`; je Phase ein PR auf `main`, weil Pages aus `main` ausliefert und jede Phase produktiv enden soll |
| Lernumgebung | `web/lab/` im BM-Repo → `swrobuts.github.io/BurgerMetrics/lab/`, ausgeliefert vom vorhandenen Workflow (`path: 'web'`); kein eigenes Repo. Hinweisdatei `Vorlesungen/Lernumgebungen/BM-Lab.md` mit dem Pfad, damit der Ordner der Lernumgebungen vollständig bleibt |
| Deck | `BurgerMetrics/BurgerMetrics_3_Analyse.pptx`, gebaut von `slides/bau_analyse.py`; PPTX nicht versioniert (Schriftlizenz) |
| Vorlage Lab | `Vorlesungen/Lernumgebungen/WInf-SP` (Laufzeit, Übungstypen, Autorenleitfaden `tools/AUTORENLEITFADEN.md`) |

**Achtung Worktree:** Eine Claude-Code-Sitzung im Ordner `BurgerMetrics/` erzeugt einen Worktree
des Elternrepos `Datenbasierte Fallstudien/.git` (Phil / Knowledge Navigator), nicht des
BM-Repos. Die Sitzung muss in `BurgerMetrics_Website` gestartet werden.

### 2.2 Datenbank

Selbstgehostete Supabase, PostgreSQL 17.6. Betreiberkonto aus der nicht versionierten `.env`
(auch für den MCP-Server `burgermetrics-db`). Demo-Konto für Studierende, absichtlich
öffentlich (`db/aufbau/0020_demo_rolle.sql`):

| | |
|---|---|
| Host | `supabase.butscher.cloud` |
| Port | `5433` |
| Datenbank | `postgres` |
| Benutzer | `studi_daba` |
| Kennwort | `thws` |
| Suchpfad | `wawi, burgermetrics` |
| Grenzen | nur lesen, 10 min je Abfrage, 20 Sitzungen, kein `bestellung_anlegen()` |

PostgREST für die Seiten: `https://supabase.butscher.cloud`, anon-Schlüssel in
`web/js/konfiguration.js`, Schema je Anfrage über `Accept-Profile` / `Content-Profile`.

### 2.3 Farben BM-Lab

Kontraste nach WCAG gegen Weiß, berechnet:

| Rolle | Wert | Kontrast |
|---|---|---|
| Leitfarbe (Flächen mit weißer Schrift, Text, Links) | `#C2410C` | 5,2:1 |
| zweite Signalfarbe (Hover, Links) | `#9A3412` | 7,3:1 |
| Akzent (Linien, Badges, Fortschritt — nie als Schriftfarbe) | `#ED7004` (THWS-Orange) | 3,0:1 |
| Aufhellung (Hinweiskästen, aktive Navigation) | `#FDF1E7` | — |
| dunkle Schrift auf Aufhellung | `#7C2D12` | 8,4:1 |

## 3 Rezensionen — Datenbank

Neue Datei `db/aufbau/0021_rezensionen.sql`, idempotent, Kopfkommentar wie die anderen
(Zweck, Voraussetzung, Objekte, Rücknahme).

### 3.1 Operativ: `wawi.rezension`

```sql
CREATE TABLE IF NOT EXISTS wawi.rezension (
  rezension_id  bigint GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
  artikel_id    bigint NOT NULL REFERENCES wawi.artikel(artikel_id),
  kunde_id      bigint REFERENCES wawi.kunde(kunde_id),
  bestellung_id bigint REFERENCES wawi.kundenbestellung(bestellung_id) ON DELETE SET NULL,
  filiale_id    bigint REFERENCES wawi.filiale(filiale_id),
  sterne        smallint NOT NULL CHECK (sterne BETWEEN 1 AND 5),
  inhalt        text NOT NULL CHECK (char_length(inhalt) BETWEEN 5 AND 500),
  erstellt_am   timestamptz NOT NULL DEFAULT now(),
  quelle        text NOT NULL CHECK (quelle IN ('simulation', 'shop')),
  sitzung       text
);
```

Indizes auf `artikel_id`, `kunde_id`, `erstellt_am`. Row Level Security mit Policy
`lesen_alle` wie alle `wawi`-Tabellen. Nach dem Bestandsimport wird die Identity auf
`max(rezension_id) + 1` gesetzt.

**Schreibfunktion** `wawi.rezension_anlegen(artikel_id bigint, sterne integer, inhalt text,
filiale_id bigint DEFAULT NULL, sitzung text DEFAULT NULL) RETURNS jsonb` — nach dem Muster von
`bestellung_anlegen()`: `SECURITY DEFINER`, `SET search_path = wawi, pg_temp`,
`#variable_conflict use_variable`. Prüfungen: Artikel bekannt, Sterne 1–5, Text nach `btrim`
5–500 Zeichen, Filiale bekannt falls angegeben. Bremse: höchstens 20 Rezensionen je Sitzung in
zehn Minuten (`ERRCODE 53400`); Aufrufe ohne `sitzung` teilen sich einen gemeinsamen Eimer; dazu eine Notbremse von 600 Shop-Rezensionen je Stunde insgesamt; `sitzung` höchstens 100 Zeichen. `quelle` ist fest `'shop'`, `kunde_id` bleibt leer (der Shop hat
kein Kundenkonto). Rückgabe `{"rezension_id", "artikel", "sterne", "erstellt_am"}`. Rechte:
`REVOKE ALL FROM PUBLIC`, `GRANT EXECUTE TO anon, authenticated`, ausdrücklich `REVOKE` für
`studi_daba`.

**Sichten:**

* `wawi.v_rezension_produkt` — je Artikel: `artikel_id`, `name`, `anzahl`, `sterne_mittel`
  (auf eine Nachkommastelle), `letzte` (Datum). Über alle Quellen.
* `wawi.v_kundenstimmen` — die drei jüngsten Rezensionen je Artikel mit `quelle = 'simulation'`
  (Sterne, Text, Datum) für die Anzeige im Shop. **Nur** Simulationstexte, nie Besuchertexte.
* `wawi.v_rezension_letzte` — die 50 jüngsten Shop-Rezensionen mit `im_warehouse` (wie
  `v_bestellung_letzte`).

**Rücksetzen:** `wawi.uebungsrezensionen_loeschen()` löscht `quelle = 'shop'` in beiden
Schemata und gibt die Zahlen zurück.

### 3.2 Analytisch: `burgermetrics.fact_reviews`

```sql
CREATE TABLE IF NOT EXISTS burgermetrics.fact_reviews (
  review_id   bigint PRIMARY KEY,
  date        date NOT NULL REFERENCES burgermetrics.dim_date(date),
  time        time,
  customer_id bigint REFERENCES burgermetrics.dim_customer(customer_id),
  product_id  bigint NOT NULL REFERENCES burgermetrics.dim_product(product_id),
  branch_id   bigint REFERENCES burgermetrics.dim_branch(branch_id),
  order_id    bigint REFERENCES burgermetrics.fact_orders(order_id),
  stars       smallint NOT NULL CHECK (stars BETWEEN 1 AND 5),
  review_text text NOT NULL,
  source      text NOT NULL CHECK (source IN ('simulation', 'shop'))
);
```

Indizes auf `date`, `product_id`, `customer_id`. Row Level Security mit Policy `lesen_alle`
wie alle Tabellen in `burgermetrics` (0004). Grain: eine Rezension. Kennzahl in der
Semantikschicht: `burgermetrics.v_rezension_produkt` (materialisiert wie die anderen `v_*`):
je Produkt `product_name`, `category`, `anzahl`, `sterne_mittel`, `anteil_positiv_pct` (4–5
Sterne), `anteil_negativ_pct` (1–2 Sterne). `materialisieren.py` nimmt die Sicht automatisch mit.

**ETL** (Erweiterung von `0019`, in `0021`): Sicht `wawi.stg_fact_reviews` (über alle Quellen, damit `etl_probe()` den vollständigen
Gleichstand prüft; Spaltennamen ins Auswertungsvokabular). `burgermetrics.uebernahme_aus_wawi()`
wird per `DROP FUNCTION` + `CREATE` um `neue_rezensionen integer` in der Rückgabe erweitert; die
Tage-Ergänzung in `dim_date` berücksichtigt auch Rezensionsdaten. `wawi.etl_probe()` bekommt
eine Zeile `rezension`.

### 3.3 Ladeweg

CSV bleibt Quelle der Wahrheit: `dataset/fact_reviews.csv` (LFS) → `db/lade_csv.py`
(`REIHENFOLGE` um `fact_reviews` ergänzt, nach `fact_order_items`) → `burgermetrics.fact_reviews`
→ `0021` kopiert die Zeilen mit `source = 'simulation'` einmalig nach `wawi.rezension`
(Muster `0017_wawi_bestand.sql`), gleiche IDs; `date` + `time` werden zu `erstellt_am` in
`Europe/Berlin`, `product_id`/`branch_id`/`customer_id` bleiben Zahlen, weil die Stammdaten in
beiden Schemata dieselben Schlüssel tragen (0016 — in der Umsetzung gegen `wawi_zu_analytisch.sql`
prüfen). Danach läuft der ETL nur für Shop-Rezensionen.

Mini-Skripte: `dataset/burgermetrics_mini.sql` und `dataset/wawi_mini.sql` bekommen je zwölf
Rezensionen zu vorhandenen Belegen, `dataset/wawi_zu_analytisch.sql` die Abbildung
`rezension → fact_reviews` — der Nachweis lautet dann „neun von neun zeilengleich“.

### 3.4 Rechte und Proben

`0021` läuft **als `postgres`** (über den MCP-Server oder `psql` mit dem Betreiberkonto), nicht als
`supabase_admin`: Die Default-Privileges aus 0020 und 0018 gelten nur für Objekte, die `postgres`
anlegt. Unabhängig davon setzt das Skript die Rechte ausdrücklich — `GRANT SELECT` auf
`wawi.rezension`, `burgermetrics.fact_reviews` und die neuen Sichten an `studi_daba`, `anon`,
`authenticated`; `ENABLE ROW LEVEL SECURITY` plus Policy `lesen_alle` auf beiden Tabellen —, damit
kein Leserecht an der Reihenfolge der Ausführung hängt. Stand 12.09.2026, live geprüft: `studi_daba`
liest alle 71 Objekte beider Schemata, schreibt keines, darf `bestellung_anlegen()` nicht ausführen
und sieht das Schema `velocity` nicht. Das Skript endet mit einem `DO`-Block, der prüft: `studi_daba` liest `wawi.rezension` und
`burgermetrics.fact_reviews`, schreibt keines, darf `rezension_anlegen()` nicht ausführen;
`anon` darf sie ausführen; Zeilenzahl `fact_reviews` = Zeilenzahl `rezension` mit
`quelle = 'simulation'` = 10.000.

## 4 Rezensionen — Generator und Glättung

### 4.1 Generator `dataset/generate_reviews.py`

Deterministisch (`random.seed(2026)`, `numpy` mit Generator und festem Seed), liest die
CSV-Dateien per DuckDB. Läuft mit `python3 dataset/generate_reviews.py` und schreibt den
Rohstand `dataset/fact_reviews_roh.csv` (LFS, versioniert); zwei Läufe liefern byte-identische
Dateien (Prüfung per Hash). Der geglättete Bestand `dataset/fact_reviews.csv` entsteht in 4.2.

**Auswahl.** 10.000 Bestellpositionen aus `fact_order_items` × `fact_orders` (alle Bestellungen
tragen `customer_id`), höchstens eine Rezension je Bestellung, keine Produkte der Kategorie
`Extra` (Saucen). Gewichte: Jahr linear steigend (2017 = 1 bis 2026 = 4),
Kanal App Order ×3, Kiosk ×1,5, sonst ×1; Kunden mit `has_app` ×2.

**Sterne.** Latente Größe `z = 1,2 · (satisfaction_score − 3,8) − 0,03 · (order_duration_min − Median)
+ produktniveau + ε`, `ε ~ N(0; 0,6)`; `produktniveau` je Produkt fest aus einer Tabelle im
Skript (z. B. Truffle Deluxe +0,4, Fish Burger −0,3, Water 0). Schwellen so gesetzt, dass etwa
5★ 38 %, 4★ 27 %, 3★ 13 %, 2★ 9 %, 1★ 13 % entstehen (Toleranz ±3 Punkte, wird geprüft).
Damit hängen Sterne messbar an Zufriedenheit und Bestelldauer der Bestellung — das ist die
Struktur, die das Sentiment-Notebook nachweisen soll.

**Datum und Zeit.** `date` = Bestelldatum + 0–3 Tage, begrenzt auf `2026-03-31` (Ende von
`dim_date`); `time` aus einer Tagesverteilung mit Abendspitze.

**Text.** Bausteine in `dataset/rezension_bausteine.json`, deutsch, je Sternestufe:
Einstieg (≥ 25 je Stufe), Produktsatz je Kategorie Burger/Side/Drink/Dessert/Breakfast (≥ 20 je
Kategorie und Polarität, mit Platzhalter `{produkt}`), Kontextsatz (Wartezeit ↔
`order_duration_min` in Klassen, Kanal ↔ `order_channel`, Filiale ↔ `branch_name` mit
Wahrscheinlichkeit 0,3, Preis ↔ Aktion/Rabatt), Schluss (Empfehlung ↔ Sterne). Ein bis vier
Sätze, 10–60 Wörter. Rund 15 % der Texte umgangssprachlich: Kleinschreibung, „ae/oe/ue“ statt
Umlaute, mehrfache Satzzeichen — **kein Emoji**. Keine echten Personen, keine Beleidigungen,
keine Markennamen außer den Produktnamen des Bestands.

**Spalten der CSV:** `review_id, date, time, customer_id, product_id, branch_id, order_id,
stars, review_text, source` (`source` = `simulation`). Texte in Anführungszeichen, UTF-8,
Zeilenumbrüche im Text nicht erlaubt.

### 4.2 Glättung in Claude Code

Nach dem Generatorlauf werden die Texte sprachlich geglättet — in der Sitzung, nicht über die
API. Ablauf:

1. `dataset/glaettung/lose_schreiben.py` teilt `fact_reviews_roh.csv` in 100 Lose à 100 Texte
   (`lose/los_001.csv` … mit `review_id, stars, review_text`).
2. Je Los ein Subagent mit festem Prompt: Text natürlicher formulieren; Bedeutung, Sterne,
   Produkt- und Filialnamen, Polarität und Stilebene (umgangssprachlich bleibt
   umgangssprachlich) erhalten; Länge ±30 %; keine neuen Fakten, keine Emojis, kein Englisch;
   Ausgabe als CSV mit `review_id, review_text`. Ergebnis nach `lose/geglaettet_001.csv`.
3. `dataset/glaettung/zusammenfuehren.py` prüft Vollständigkeit (jede `review_id` genau
   einmal), Längen (5–500 Zeichen, ±30 % zum Original), keine Zeilenumbrüche, und schreibt
   `dataset/fact_reviews.csv`. Lose werden nicht versioniert (gitignore); Rohstand und
   geglätteter Bestand liegen beide im Repo, geladen wird nur der geglättete.
4. Stichprobe: 50 zufällige Texte werden gegen ihre Sterne gelesen; Abweichungen werden
   korrigiert oder das Los wiederholt.

Abbruchkriterium: Reicht die Zeit nicht für 100 Lose, werden die Lose in zufälliger
Reihenfolge geglättet und der Stand im README genannt (z. B. „6.000 von 10.000 geglättet“).

### 4.3 Dokumentation

`dataset/README.md`: Abschnitt `fact_reviews.csv` (und `fact_reviews_roh.csv`) mit
Feldbeschreibung, Herkunft (Generator + Glättung), Kontrollzahlen (10.000 Zeilen, Sterneverteilung, Ø Sterne, Anteil mit Filiale).
`dataset/verify_readme.py`: die neuen Zahlen werden mitgeprüft.

## 5 Shop-Feature (`web/shop.html`, `web/js/datenquelle.js`)

* `datenquelle.js`: neue Methoden in `Datenquelle` (abstrakt) und `PostgrestQuelle`:
  `rezensionenProdukt()` → `v_rezension_produkt`, `kundenstimmen()` → `v_kundenstimmen`,
  `rezensionAnlegen(r)` → `rufe('rezension_anlegen', r)`, `letzteRezensionen()` →
  `v_rezension_letzte` (frisch).
* Am Produkt (Karte und Detail): „★ 4,3 · 128 Bewertungen“ aus `v_rezension_produkt`; ohne
  Rezension „noch keine Bewertung“. Knopf **Bewerten** öffnet ein Modal: fünf Sterne
  (Radio-Gruppe, Tastatur bedienbar), Textfeld mit Zähler (5–500), Filialwahl (vorbelegt aus
  `cartBranch`), Senden. Unter dem Formular die drei Kundenstimmen aus `v_kundenstimmen`.
* Speichern über `window.rezensionSpeichern` (Modulblock, mit `sitzung: SITZUNG`) →
  `rezensionAnlegen`. Bestätigung: „Gespeichert als `wawi.rezension #4711`“, Ø am Produkt wird
  neu geladen. Fehler werden mit der Meldung der Datenbank angezeigt (z. B. Bremse).
* Datenmodus: eine Anmerkung „Rezension → `wawi.rezension` → ETL → `fact_reviews`“ im Stil
  der vorhandenen Datenanmerkungen; die zuletzt gespeicherte Rezension erscheint als Datensatz.
* Sicherheit: Besuchertexte werden nirgends auf der Seite gerendert; alles, was angezeigt wird,
  kommt aus `v_kundenstimmen` (Simulation) und wird mit `textContent` gesetzt. Längen- und
  Ratenbegrenzung liegen in der Datenbank.
* `web/abgleich.html` bleibt unverändert; `web/dashboard.html` bekommt **keine** Rezensionskarte.

## 6 Notebooks (`notebooks/`)

### 6.1 Konventionen

* Jedes Notebook beginnt mit denselben Abschnitten: **Fragestellung · Daten · Vorgehen ·
  Ergebnis · Was offen bleibt.** Sachlicher Ton, ganze Sätze, keine Bilder oder Pointen — auch in
  `print()`-Ausgaben.
* Eine **Verbindungszelle** je Notebook, keine versteckten Module:

  ```python
  import pandas as pd
  from sqlalchemy import create_engine

  VERBINDUNG = "postgresql+psycopg2://studi_daba:thws@supabase.butscher.cloud:5433/postgres"
  engine = create_engine(VERBINDUNG)

  def lade_sql(sql):
      # Führt eine Abfrage aus und gibt das Ergebnis als DataFrame zurück
      return pd.read_sql(sql, engine)

  def lade_csv(name):
      # Liest eine CSV-Datei des Datensatzes direkt aus GitHub (Git LFS)
      url = f"https://media.githubusercontent.com/media/swrobuts/BurgerMetrics/main/dataset/{name}.csv"
      return pd.read_csv(url)
  ```

  Große Tabellen (`fact_orders`, `fact_order_items`, `obt_orders`) werden aus der Datenbank
  gelesen, `lade_csv` nur für Dimensionen und zur Demonstration — das schont das LFS-Kontingent.
* Code für Anfänger: kurze Funktionen, deutsche Bezeichner, ein Kommentar je Funktion, keine
  verketteten Einzeiler, Ergebnisse als DataFrame anzeigen statt drucken.
* Diagramme mit matplotlib (Standardfarben, beschriftete Achsen, Nullpunkt bei Länge und Fläche),
  keine Wortwolken.
* Am Kopf ein „In Colab öffnen“-Link und eine `pip`-Zelle (`sqlalchemy psycopg2-binary duckdb
  scikit-learn mlxtend statsmodels holidays dash`); `notebooks/requirements.txt` daneben.
* Ausgeführt eingecheckt (GitHub rendert die Ergebnisse); `notebooks/pruefe_notebooks.sh`
  führt alle per `jupyter nbconvert --execute --to notebook --inplace` aus und ist die Abnahme.

### 6.2 Die Notebooks

| Datei | Inhalt |
|---|---|
| `00_zugang_und_daten.ipynb` | Verbindung als `studi_daba`; Schemata und Sichten über `information_schema`; Suchpfad; erste Kennzahlen aus `v_kennzahlen_jahr`; CSV per LFS-URL; DuckDB auf CSV (`load_duckdb.py`); Export als Parquet/CSV für Power BI und Tableau; Grenzen der Rolle; zum Schluss die Dash-App inline (6.3) |
| `01_analytisches_datenmodell.ipynb` | Schrittweise: Frage → Grain → Dimensionen → Fakten → Sicht. Auszug aus `wawi` (3NF) nach DuckDB, dort `dim_product` aus drei Tabellen, `fact_orders`, `fact_order_items`, eine Sicht `v_umsatz_filiale_jahr`; Fan Trap vorführen (70,8 Mio. statt 14,5 Mio.); Abgleich mit `burgermetrics`; Rezensionen als neue Faktentabelle |
| `02_rfm_und_kundensegmente.ipynb` | RFM je Kunde aus `fact_orders`, Quintile mit Zweitschlüssel; Vergleich mit `v_rfm_kunde`; K-Means auf standardisierten RFM-Werten, Elbow und Silhouette; Segmente beschreiben |
| `03_warenkorbanalyse.ipynb` | Warenkörbe aus `fact_order_items`; Apriori/FP-Growth (`mlxtend`); Support, Konfidenz, Lift; Richtung der Regel; Vergleich mit `v_warenkorb_regeln` (Lift 1,03 als schwaches Signal) |
| `04_nachfrageprognose.ipynb` | Tagesumsatz je Filiale; STL-Zerlegung (`statsmodels`); Merkmale aus `dim_date` und `dim_weather`; lineare Regression und Gradient Boosting; Backtesting über die letzten zwölf Monate; MAE/MAPE; Basiseffekt bei Rumpfjahren |
| `05_wetter_und_ereignisse.ipynb` | Effekte auf `dim_weather` und `special_event` (Regression Umsatz ~ Temperatur, Regen, Wochentag, Feiertag, Event); danach externe Quellen (Abschnitt 7): Open-Meteo, Feiertage, Schulferien, Kickers-Heimspiele, VPI; Verknüpfung über das Datum; Vergleich `dim_weather` gegen gemessenes Wetter (Korrelation nahe null — synthetischer Bestand, ehrlich benannt) |
| `06_zufriedenheit_erklaeren.ipynb` | `satisfaction_score` in Klassen (niedrig < 3,5, mittel, hoch ≥ 4,2); Entscheidungsbaum und Random Forest mit Kanal, Bestelldauer, Filiale, Stunde, Wochentag; Merkmalswichtigkeit; ein gezeichneter Baum mit Tiefe 3 |
| `07_ausreisser_tagesumsatz.ipynb` | Tagesumsätze je Filiale; z-Score und IQR; Isolation Forest; Treffer erklären (Eröffnung, Feiertag, Event, Kiliani) |
| `08_sentiment_rezensionen.ipynb` | `fact_reviews`: Vorverarbeitung (Kleinschreibung, Umlaute, Stoppwörter); Wortliste als Grundlinie; TF-IDF + logistische Regression, Sterne → Polarität (1–2 negativ, 4–5 positiv), Konfusionsmatrix; vortrainiertes deutsches Modell `oliverguhr/german-sentiment-bert` auf einer Stichprobe zum Vergleich; Sentiment gegen Kanal, Bestelldauer, Filiale, Produkt |

### 6.3 Dash-App (`dash/app.py`, `dash/requirements.txt`, `dash/README.md`)

Plotly Dash, eine Datei, rund 150 Zeilen: Kennzahlkacheln (`v_kennzahl_einzeln`), Umsatz je
Monat als Linie mit Filialfilter (`v_umsatz_monat`, `v_filiale`), Kanalanteile je Jahr
(`v_kanal_jahr`), Ø Sterne je Produkt (`v_rezension_produkt`). Verbindung über
`DATABASE_URL` aus der Umgebung, Vorgabe `studi_daba`. Start `python dash/app.py` →
`http://127.0.0.1:8050`. Die Colab-Zelle mit `app.run(jupyter_mode="inline")` steht als
letzter Abschnitt in `00_zugang_und_daten.ipynb`.

## 7 Externe Datenquellen

| Quelle | Zugriff | Schlüssel | Lizenz / Hinweis | Verwendung |
|---|---|---|---|---|
| Open-Meteo Historical Weather | `archive-api.open-meteo.com/v1/archive`, Würzburg 49,79 N / 9,95 O, täglich `temperature_2m_max/min`, `precipitation_sum` | keiner | Daten CC BY 4.0; API für nicht-kommerzielle Nutzung kostenfrei | gemessenes Wetter 2017–2026, Vergleich mit `dim_weather` |
| Feiertage Bayern | Python-Paket `holidays` (`country="DE", subdiv="BY"`) | keiner | MIT | Abgleich mit `dim_date.is_holiday` |
| Schulferien Bayern | `ferien-api.de` | keiner | frei, Verfügbarkeit im Notebook abfangen | Ferienmerkmal |
| Würzburger Kickers, Heimspiele | OpenLigaDB (`api.openligadb.de`) — Abdeckung der 3. Liga / Regionalliga Bayern **zu prüfen**; sonst manuell gepflegte CSV | keiner | frei | Ereignismerkmal |
| Verbraucherpreisindex | Destatis GENESIS (Tabelle 61111-0002) als CSV-Download oder API mit Anmeldung | API: Konto | Datenlizenz Deutschland 2.0 | Umsatz real statt nominal |

Alle Angaben der Tabelle werden zu Beginn von Phase 4 gegen die Anbieter geprüft und mit Stand
zitiert. Jede API-Antwort wird einmal geholt und als CSV unter `notebooks/daten_extern/` versioniert;
die Notebooks lesen zuerst die Datei und rufen die API nur, wenn sie fehlt oder
`AKTUALISIEREN = True` gesetzt ist. So laufen sie auch ohne Netz und ohne Kontingent.

## 8 Foliendeck `BurgerMetrics_3_Analyse.pptx`

Bauskript `slides/bau_analyse.py` mit `deckwerk.py`, Vorlage und Bausteine aus dem Skill
`thws-slides`; Kette des Skills: konzipieren (ein Satz je Folie), recherchieren (Werkzeugangaben
gegen Herstellerdokumentation, mit Stand), skizzieren, bauen, normalisieren, prüfen, ausliefern.
Regeln: Aussagentitel einzeilig, Einleitung drei bis vier Zeilen, Quellenzeile immer befüllt,
keine Terminbindung, keine Übungsankündigungen, nie dasselbe Motiv auf zwei Folien nacheinander,
Schrift nie unter 13 pt. Diagramme als Mermaid unter `slides/diagramme/` (Nummern ab `50`),
Bildschirmfotos über `slides/screenshots.mjs` (Shop-Bewertung, Dash, BM-Lab).

Kapitel und Zielumfang (42 Inhaltsfolien; mit Deckblatt, Agenda und fünf Kapitelfolien rund 50):

| Kapitel | Inhalt | Folien |
|---|---|---|
| 1 Zugang | ein Konto, zwei Schemata; Werkzeuge psql, DBeaver, DataGrip; Suchpfad und Präfix; Grenzen der Rolle; erste Abfragen; Sichten `v_*` als Vertrag | 7 |
| 2 Daten bereitstellen | CSV und LFS; DuckDB; Power BI (PostgreSQL-Connector, Import gegen DirectQuery, Beziehungen aus Fremdschlüsseln); Tableau (Connector, Live gegen Extract); Python (SQLAlchemy, pandas); Exportformate | 9 |
| 3 Analytisches Datenmodell schrittweise | Frage → Grain → Dimensionen → Fakten → Sicht; wawi nach DuckDB; Kennzahl genau einmal; Fan Trap; Rezensionen als neue Faktentabelle und ihr ETL-Weg | 9 |
| 4 Data Mining | Übersicht der Notebooks; je Verfahren eine Folie mit Frage, Verfahren, Kennzahl, Befund: RFM/K-Means, Warenkorb, Prognose, Zufriedenheit, Ausreißer, Sentiment; ehrliche Interpretation | 9 |
| 5 Externe Daten | Wetter (synthetisch gegen gemessen), Kalender, Ereignisse, Preise; Verknüpfungsschlüssel Datum; Lizenz und Zitierpflicht; ein API-Aufruf | 6 |
| Abschluss | Dash-App, BM-Lab, Wege weiter | 2 |

Prüfung: `sprache_check.py --whitelist slides/fachbegriffe.txt`, `trennstrich_check.py`,
`einleitung_budget.py`, `formsprache_audit.py`, Render per `soffice`-Wrapper, Seitenzahl der
PDF gegen Folienzahl. `slides/README.md` bekommt die dritte Zeile der Tabelle und die neuen
Diagramme.

## 9 BM-Lab

### 9.1 Aufbau

```
web/lab/
  index.html                 Übersicht: acht Kacheln, Gesamtfortschritt, Verweise auf Startseite, Shop, Dashboard, Notebooks
  lab-01-zugang.html         Zugang und Datenbank
  lab-02-daten.html          Daten beschaffen
  lab-03-datenmodell.html    Analytisches Datenmodell
  lab-04-powerbi.html        Power BI
  lab-05-tableau.html        Tableau
  lab-06-python.html         Python und Colab
  lab-07-datamining.html     Data Mining
  lab-08-extern.html         Externe Daten und Sentiment
  assets/bm.css              aus winf.css, Palette Abschnitt 2.3, Kopfkommentar angepasst
  assets/bm.js               aus winf.js: LABS, localStorage-Schlüssel bm:*, nur Deutsch — Sprachumschalter entfernt, Texte nur als de (HTML: <span lang="de">, JSON: {"de": …}); die Laufzeit muss de zeigen, wenn en fehlt
  assets/jsonpruefung.js unverändert, regal.js mit zwei angepassten Meldungen (pruefung.js entfällt, siehe Stand Phase 6)
  assets/pglite/             übernommen (PostgreSQL im Browser)
  data/burgermetrics_mini.sql, data/wawi_mini.sql   Kopien aus dem BM-Repo, mit Rezensionen
  data/regal-bestellungen.json   2.000 Zeilen Stichprobe aus obt_orders: monat, wochentag, stunde, filiale, kanal, zahlart, loyalty, saison, order_id, umsatz, positionen, zufriedenheit
  data/uebungen/lab-0N.json  Befehlskarten, Übungen, Spielplätze je Lab
  vorlagen/                  Kopiervorlagen: Verbindungszeilen, Measures, Calculated Fields, Notebook-Zelle
  tools/verify.mjs, tools/sql.mjs   aus WInf-SP; Zweisprachigkeitsprüfung durch Deutsch-Prüfung ersetzt; von keiner Seite referenziert
  tools/gen_daten.py          kopiert die Mini-Skripte, schreibt data/regal-bestellungen.json (Demo-Konto)
  README.md                  Aufbau, Konventionen, Abnahmelauf (Autorenhinweise)
```

Alles liegt unter `web/`, weil der Pages-Workflow genau dieses Verzeichnis ausliefert; `web/.nojekyll`
besteht bereits. Die PGlite-Assets (rund 19 MB) werden ins Repo gelegt, nicht vom CDN geladen, damit
die SQL-Übungen im Hörsaal an keinem fremden Dienst hängen; das Pages-Paket wächst von 530 KB auf
rund 20 MB, was für Pages unkritisch ist.

Terminal- und Deploy-Simulator werden nicht übernommen (kein Lab braucht sie); `terminal.js`,
`deploy.js`, `sqljs/` entfallen. Die Seitendatenbank ist überall `data-datenbank="postgres"`.

### 9.2 Labs und Übungen (je fünf bis sechs, rund 45)

| Lab | Abschnitte (Dramaturgie des Leitfadens) | Übungen |
|---|---|---|
| 01 Zugang | Warum eine Rolle; Verbindungsdaten; Werkzeuge (Nachbildung DBeaver/DataGrip-Dialog); Schemata und Suchpfad; Sichten als Vertrag; Grenzen; Stolpersteine (Präfix, Timeout, „permission denied“) | `sql` ×3 auf mini, `quiz`, `zuordnen`, `checkliste` (echte Verbindung herstellen) |
| 02 Daten beschaffen | CSV/LFS (Zeigerdatei erkennen); DuckDB; Export nach Parquet; was in welches Werkzeug | `sql` ×2 auf mini (Abfragen, die in DuckDB und PostgreSQL gleich laufen), `reihenfolge`, `quiz`, `zuordnen` |
| 03 Datenmodell | Frage → Grain → Dimensionen → Fakten; die Schritte werden in PGlite gebaut (`CREATE TABLE AS`, `CREATE VIEW`); Fan Trap; Rezensionen anbinden | `sql` ×4 mit `kontrolle`, `quiz` |
| 04 Power BI | aus WInf-SP Lab 12 übertragen: Verbindung PostgreSQL, Modell aus Fremdschlüsseln, DAX-Measures (`Umsatz`, `AOV`, `Ø Sterne`), Bericht; Shelf-Simulator (Power BI: Field Wells, Tableau: Shelves) mit BM-Feldern | `regal` ×2, `quiz`, `zuordnen`, `checkliste` |
| 05 Tableau | aus Lab 13: Connector, Dimension/Measure, diskret/stetig, LOD, Dashboard | `regal` ×2, `quiz`, `reihenfolge`, `checkliste` |
| 06 Python und Colab | aus Lab 04: Notebook, Runtime, Verbindungszelle, pandas, Dash inline; Zugangsdaten nie ins Repo | `quiz` ×2, `zuordnen`, `reihenfolge`, `checkliste` (Notebook 00 ausführen) |
| 07 Data Mining | je Verfahren: Frage, Vorgehen, Kennzahl, Fallstrick; Verweise auf Notebooks 02–07 | `zuordnen` (Frage → Verfahren), `quiz` ×2, `sql` (RFM-Grundlage), `reihenfolge` (CRISP-DM-Schritte) |
| 08 Extern und Sentiment | Datum als Schlüssel; Open-Meteo-Aufruf; Lizenz; Rezension im Shop schreiben und in `v_rezension_letzte` wiederfinden; Sentiment-Verfahren | `json` (Open-Meteo-Antwort), `sql` ×2 (Join über Datum, Sterne je Produkt), `checkliste` (Shop → Datenbank), `quiz` |

Alle Übungen sind aus dem Text lösbar, mit `hinweis` und `loesung`; SQL-Lösungen werden mit
`node tools/sql.mjs postgres "…"` gegen die Browser-Datenbank geprüft, bevor sie ins JSON kommen.

### 9.3 Veröffentlichen

Kein eigenes Setup: Der Merge auf `main` löst den vorhandenen Workflow `static.yml` aus, der `web/`
samt `web/lab/` ausliefert. Lokal wie bei den anderen Seiten: `cd web && python3 -m http.server 8731`
→ `http://localhost:8731/lab/`. Abnahme: `node web/lab/tools/verify.mjs` ohne Befund, Sichtprüfung
jeder Seite im Browser (Kopf, Navigation, Übungen, Datenbankband), Aufruf von
`https://swrobuts.github.io/BurgerMetrics/lab/` nach dem Deploy.

## 10 Verknüpfungen und Dokumentation im BM-Repo

* `web/index.html`: vierte Kachel `c-orange` „Lernumgebung BM-Lab“ → `lab/` (relativ, funktioniert lokal und auf Pages)
  (Kartenraster auf vier Spalten, ab 1024 px zwei, mobil eine); Tastenkürzel `4`; im Abschnitt
  Analytics-Projektablauf ein Satz zu Notebooks und Dash mit Link ins Repo.
* `README.md`: Abschnitte „Notebooks und Dash“ und „Lernumgebung“ (Adresse
  `swrobuts.github.io/BurgerMetrics/lab/`); Tabelle „Was wo liegt“ um `notebooks/`, `dash/` und
  `web/lab/` ergänzt; Rezensionen in „Die Kette“ (Schritt 4 und 5).
* `db/README.md`: Zeile `0021`, Rezensionsweg, `uebungsrezensionen_loeschen()`.
* `docs/02-datenmodell.md` (fact_reviews im Galaxy-Schema), `docs/05-anwendungen.md`
  (Shop-Feature), `docs/07-lehrbezug.md` (Modul Datenbasierte Fallstudien, Notebooks, BM-Lab),
  `docs/08-entscheidungen.md` (Rezensionen über CSV, Besuchertexte nicht öffentlich,
  Generator + Glättung).
* `slides/README.md`: dritte Zeile, neue Diagramme und Bildschirmfotos.
* `web/lab/index.html` verweist zurück auf die Startseite (`../`), Shop, Dashboard und die
  Notebooks im Repo; `web/lab/README.md` nennt Aufbau und Abnahmelauf.
* `Vorlesungen/Lernumgebungen/BM-Lab.md` (außerhalb des Repos): drei Zeilen mit Pfad und Adresse.

## 11 Abnahmekriterien

| Teil | Nachweis |
|---|---|
| `0021` | läuft zweimal fehlerfrei; Proben-Block meldet 10.000 Zeilen, Rechte wie in 3.4; `etl_probe()` 0/0 auch für `rezension`; `materialisieren.py --neu` läuft |
| Generator | zwei Läufe gleicher Hash für `fact_reviews_roh.csv`; Verteilung innerhalb der Toleranz; `verify_readme.py` Exit 0 |
| Glättung | `zusammenfuehren.py` ohne Befund; Stichprobe 50 gelesen |
| Shop | im Browser eine Rezension gespeichert, in `v_rezension_letzte` sichtbar, Ø am Produkt aktualisiert; `uebungsrezensionen_loeschen()` räumt auf; Datenmodus zeigt den Datensatz |
| Notebooks | `pruefe_notebooks.sh` läuft alle neun ohne Fehler gegen die Datenbank; Ausgaben eingecheckt |
| Dash | startet lokal, vier Karten mit Daten; Screenshot für das Deck |
| Deck | Prüfskripte ohne offenen Befund, Render angesehen, PDF-Seitenzahl = Folienzahl, Quellenzeilen vollständig |
| BM-Lab | `verify.mjs` ohne Befund; jede SQL-Lösung geprüft; Pages erreichbar; Verweise in beide Richtungen funktionieren |
| Übergabe je Phase | Stand ist produktiv, ohne dass Robert etwas ausführt: `0021` auf der Instanz eingespielt und `fact_reviews` gefüllt; `materialisieren.py --neu` gelaufen; Shop auf Pages mit Bewertungsfunktion; Notebooks mit Ausgaben auf GitHub; BM-Lab veröffentlicht; `README.md` und `web/index.html` verweisen darauf |

**Stand 12. September 2026 (Phase 2):** `0021`, Generator und Glättung sind abgenommen; von der Übergabe je Phase ist der Datenbankteil erfüllt (`0021` eingespielt, `fact_reviews` und `wawi.rezension` mit 10.000 Zeilen, `v_rezension_produkt` materialisiert, PostgREST neu gestartet). Shop, Notebooks, Dash, Deck und BM-Lab folgen in den Phasen 3–6.

**Stand 12. September 2026 (Phase 3):** Shop abgenommen — Rezension im Browser gespeichert, in `v_rezension_letzte` sichtbar, Ø am Produkt aktualisiert, `uebungsrezensionen_loeschen()` räumt auf, Datenmodus zeigt den Datensatz. „Karte und Detail“ in Abschnitt 5 meint Karte und Modal — der Shop hat keine Produktdetailseite. Die Bremse (`ERRCODE 53400`) erreicht den Browser als HTTP 503 mit der Meldung der Datenbank; die Dokumentation in Phase 7 nennt das. Von der Übergabe je Phase ist damit auch „Shop auf Pages mit Bewertungsfunktion“ erfüllt, sobald der PR auf `main` ist.

**Stand 13. September 2026 (Phase 4):** Notebooks und Dash abgenommen — `pruefe_notebooks.sh` führt alle neun gegen die Datenbank aus, die Ausgaben sind eingecheckt; Dash-App mit vier Karten und Test. Abweichungen: Kennzahlkacheln aus `v_kennzahlen_jahr` plus Zufriedenheit aus `v_kennzahl_einzeln`; Filialfilter über `fact_orders`; Schulferien über OpenHolidays statt ferien-api.de (HTTP 429); Kickers-Heimspiele nur für die Saisons mit OpenLigaDB-Abdeckung (2016/17 bis 2021/22); VPI als kuratierte CSV mit Quellenangabe; Fan Trap auf dem Auszug 2025 statt des ganzen Bestands; vierte Kopfzelle `zahl()` für deutsche Zahlen; RFM-Werte log-transformiert für K-Means; Regressionen in 05 mit Referenzkategorien, Saisonkontrolle und Vergleichstagen statt Heimspiel-Koeffizient; `notebooks/daten_extern/` ohne Git LFS mit GitHub-Fallback für Colab. Von der Übergabe je Phase ist „Notebooks mit Ausgaben auf GitHub“ erfüllt, sobald der PR auf `main` ist.

**Stand 13. September 2026 (Phase 5):** Deck abgenommen — `slides/bau_analyse.py` baut `BurgerMetrics/BurgerMetrics_3_Analyse.pptx` mit 50 Folien (42 Inhaltsfolien nach der Kapiteltabelle in §8); Bauskript ohne Warnungen; `sprache_check.py` (mit `slides/fachbegriffe.txt`), `trennstrich_check.py`, `einleitung_budget.py` und `formsprache_audit.py` ohne offenen Befund (Restbefunde als Fehlalarme begründet in `slides/pruefbericht_analyse.md`; Vielfaltsquote 0,62, keine Läufe); Folientext nach dem Skill `folientext-pruefen` geprüft (17 Textänderungen dokumentiert); Render mit LibreOffice und mit PowerPoint (`slides/render_ppt.applescript`) angesehen, PDF-Seitenzahl 50 = Folienzahl, Quellenzeilen vollständig. Abweichungen und Befunde: (1) Bildschirmfotos von DBeaver, DataGrip, Power BI und Tableau entfallen (keine Anmeldung in Oberflächen aus der Sitzung); diese Werkzeuge stehen als Zuordnungen mit Stand und Quelle (`slides/quellen_analyse.md`). (2) Das Bildschirmfoto des BM-Lab folgt mit Phase 6; die Abschlussfolie nennt das Lab mit seiner Adresse. (3) Git LFS bietet laut GitHub Docs (Stand 13.09.2026) 10 GiB Bandbreite je Monat, nicht 1 GB wie in §13 angenommen — die Folgerung (Notebooks lesen große Tabellen aus der Datenbank) bleibt. (4) Im Master dieses Skills passt eine Einleitung in PowerPoint nur dreizeilig (Inhaltskante y = 172); sieben Einleitungen wurden nach Stufe 2 der Kaskade gekürzt, `slides/messe_einleitungen.py` misst das im PowerPoint-PDF. (5) Auf der Grain-Folie ersetzt eine Gegenüberstellung mit Bestellung 457 das Diagramm `06_granularitaet`, das unter dem Stufenband nicht lesbar war. (6) Heimspieltage: 97 Heimspiele der Kickers liegen im Bestandszeitraum, 81 zählen in der Gegenprobe von Notebook 05. (7) Das Paket `holidays` steht unter MIT-Lizenz und ist in Belegliste und Lizenzfolie ergänzt. (8) Das Endreview fand einen Überlauf auf der Konto-Folie und elf Text- und Belegkorrekturen; alle sind in `slides/pruefbericht_analyse.md`, Abschnitt 8, dokumentiert; der committete Plan trägt den Wortlaut vor der Folientext-Prüfung. Deck und `slides/` bleiben wie die Decks 1 und 2 lokal (Schriftlizenz).

**Stand 15. September 2026 (Phase 6):** BM-Lab abgenommen — `web/lab/` mit acht Labs und 41 Übungen (6/5/5/5/5/5/5/5), Laufzeit `assets/bm.js`/`assets/bm.css`/`assets/jsonpruefung.js`/`assets/regal.js` aus WInf-SP (nur Deutsch, `bm:*`-Schlüssel, ohne Terminal-, Deploy- und SQLite-Pfade), PGlite mit den Schemata `wawi` und `burgermetrics` aus den Mini-Skripten plus `data/bm_sichten.sql`; `node web/lab/tools/verify.mjs` meldet 782 Zusicherungen, 0 Fehler (prüft Deutsch-Pflicht, Struktur, Lösbarkeit und führt jede SQL-Lösung gegen die Browser-Datenbank aus); jede Seite im Browser gesehen, eine SQL-Übung mit der Musterlösung gelöst und die Fortschrittsanzeige bestätigt; Paket unter `web/lab/` rund 20 MB, davon PGlite 19 MB; Pages liefert `web/lab/` nach dem Merge. Abweichungen: (1) `assets/pruefung.js` wurde nicht kopiert, weil die Datei nur die Terminal-Prüfungen enthält und ohne Terminal-Simulator keinen Verbraucher hat. (2) PGlite 0.5.5 liefert PostgreSQL 18.3, nicht 17 wie im Plan angenommen; die Startseite und die README nennen PostgreSQL 18. (3) `bm.css` weicht von der Palettennutzung der Vorlage ab: Dort steht der Akzent stellenweise als Schrift auf der Leitfarbe (Kontrast 1,7:1); hier ist der Akzent nach Spec §2.3 nie Schriftfarbe. (4) `dataset/burgermetrics_mini.sql` schreibt Booleans als TRUE/FALSE, weil PostgreSQL 0/1 ablehnt; DuckDB läuft mit beiden Schreibweisen. (5) Die Shelf-Daten stammen aus `obt_orders` (2.000 Bestellungen 2025) mit den Feldern Monat, Wochentag, Stunde, Filiale, Kanal, Zahlart, Loyalty-Stufe, Saison, Umsatz, Positionen und Zufriedenheit — ohne Kategorie, weil `obt_orders` je Bestellung keine trägt, erzeugt von `tools/gen_daten.py`, das §9.1 nicht nennt. (6) Lab 03 hat fünf Übungen (vier SQL, ein Quiz); die Fan-Trap-Übung ist eine SELECT-Abfrage mit dem Ergebnis 649,16 € gegenüber den korrekten 200,85 €. (7) Lab 05 deckt den Stolperstein zu Tableau und Nur-Lese-Transaktionen (§13) mit den PostgreSQL-Meldungen ab; der Tableau-Dialog selbst wird nur mit den in der Quellenliste belegten Feldern nachgebildet, nicht darüber hinaus. (8) Am 15.09.2026 wurden die Korrekturen des Betreibers auf `main` (Rechte von `studi_daba` über `db/betrieb/studi_daba_lesend.sql`, Notebook-pip-Zelle über `subprocess`, Schreibprobe mit SQLSTATE 42501, `load_duckdb.py`, Shop-Skripte, alle Notebooks neu ausgeführt) in den Zweig gemergt; Labs 01, 02, 05 und 06 wurden dafür an 24 Stellen angepasst (Zwei-Schranken-Modell aus Nur-Lese-Standardtransaktion und ACLs; `public.xyz` liefert „permission denied for schema public“; kein TEMP-Recht); Lab 07 zitiert das neu ausgeführte Notebook 04 (MAE Gradient Boosting 168,9 €). (9) Plantexte, die den Quellen widersprachen, wurden berichtigt: `load_duckdb.py` bricht bei LFS-Zeigern ab (W02-03), die Bandbreite zählt gegen den Repository-Besitzer (W02-04), die Dash-Zelle in Notebook 00 startet eine einzelne Karte (W06-05), ein Laufzeit-Neustart in Colab behält installierte Pakete (W06-01), dazu die psycopg2-Meldung zum Host-Tippfehler, die fehlende Spalte `quelle` in `v_rezension_letzte` der echten Datenbank (W08-04) und der Typfehler beim Join von Text gegen Datum (W08-02); unbelegte Oberflächentexte (Power-BI-Meldung von Npgsql, Tableau „To a File“/„Username and Password“) sind paraphrasiert oder entfallen. (10) Startseiten-Kachel, `README.md`-Verweis und weitere Doku-Verknüpfungen folgen in Phase 7.

**Stand 15. September 2026 (Phase 7):** Verknüpfungen abgenommen — `web/index.html` erhält die vierte Kachel `c-orange` „BM-Lab“ mit Verweis auf `lab/` im vierspaltigen Kachelraster, das ab 1024 px auf zwei und mobil auf eine Spalte fällt, dazu das Tastenkürzel `4` und einen Satz zu den neun Notebooks unter `notebooks/` und der Dash-App unter `dash/` auf GitHub; die Seiten-Importe von `pos.html`, `dashboard.html`, `abgleich.html` und `shop.html` tragen einheitlich den Versionsparameter `?v=20260915`. `README.md` nennt unter „Was wo liegt“ die Zeilen `web/` und `web/lab/`, ergänzt die Abschnitte „Notebooks und Dash“ und „Lernumgebung“ und schreibt in „Die Kette“ die Schritte 4 und 5 fort; `db/README.md` ergänzt die Bremse `ERRCODE 53400` um die HTTP-503-Antwort von PostgREST; `docs/02-datenmodell.md` zeigt `fact_reviews` als dritte Faktentabelle im Galaxy-Diagramm (jetzt drei statt zwei); `docs/05-anwendungen.md` beschreibt die Funktion „Bewerten“; `docs/07-lehrbezug.md` erhält den neuen Abschnitt 7.7, die bisherige Terminologie rückt auf 7.8; `docs/08-entscheidungen.md` erhält den Eintrag E10; dazu aus dem Endreview von Phase 6 `dash/README.md`, `.env.example` (`DATABASE_URL`) und der Baum in Spec §9.1 (`gen_daten.py`, `pruefung.js`); `slides/README.md` (§10) entfällt — kein Foliendeck in Phase 7, `slides/` ist nicht versioniert. Die Texte der neun BM-Lab-Seiten sind nach der Regel des Skills `folientext-pruefen` durchgesehen — 40 Sätze gestrichen oder ersetzt, danach fünf Nachbesserungen und in vier Übungsdateien fünf Rückmeldungen und Erklärungen; Aufgaben, Fragen und Optionen blieben unverändert, `verify.mjs` meldet unverändert 782 Zusicherungen, 0 Fehler. Kein Foliendeck in Phase 7 (Roberts Entscheidung vom 15.09.2026). Der Abnahmelauf bestätigt das: `node --test web/tests/*.test.mjs` 19 von 19, `skripte_pruefen.py` ohne Befund, `verify.mjs` 782 Zusicherungen und 0 Fehler, die Link-Prüfung ohne fehlendes Ziel, im Browser vier Kacheln mit funktionierender Taste `4`, der Rückweg „← Zur Fallstudie“ aus `lab/` und `shop.html` mit `?v=20260915` in den Netzwerk-Anfragen, durchweg ohne Konsolenfehler. Abweichung: Der Faktenblock des Plans nannte die öffentliche Sicht zunächst nur als Aggregat `v_rezension_produkt`; dass der Shop zusätzlich die drei jüngsten Simulationstexte je Artikel aus `v_kundenstimmen` zeigt, wurde nachträglich in den Plan aufgenommen. Damit ist die Übergabe je Phase aus §11 vollständig erfüllt, sobald der PR auf `main` ist: `README.md` und `web/index.html` verweisen auf Notebooks, Dash und BM-Lab. Nachtrag 15.09.2026: „Regal“ durch die Originalbegriffe Shelf (Tableau) und Field Well (Power BI) ersetzt, „Pille“ durch Pill; Bezeichner (`regal`, `data-regal`, `regal.js`) unverändert.

**Stand 19. September 2026 (Prüfung von Lab 05 mit Tableau Desktop 2026.2):** Nach Roberts Einzellizenz wurde Lab 05 gegen Tableau Desktop 2026.2 (macOS, deutsche Oberfläche, PostgreSQL 17.6 auf `supabase.butscher.cloud`) mit dem Demo-Konto durchgespielt: Verbindung, Extract, zwei Blätter, Filter für alle Blätter der Datenquelle, Dashboard mit Titel, die Calculated Fields `Bestellwert` (21,93 für 2025), `Umsatz je Filiale (FIXED)` und der CSV-Weg mit `notebooks/export/`; Kontrollzahl 2.994.771 in beiden Wegen. Befunde und Folgen im Lab: (1) Der Stolperstein zu Nur-Lese-Transaktionen tritt als stille Fähigkeitsprüfung beim Verbinden auf (§13, Wortlaut jetzt belegt); im Fenster erscheint keine Meldung. (2) Der Connector hat keine Schemaauswahl — die Tabellenliste zeigt jede Tabelle der Datenbank `postgres` als `tabelle (schema.tabelle)`, auch unlesbare; das Suchfeld ersetzt die Auswahl; `Initial SQL` wirkt nur auf eigene Abfragen (Custom SQL ohne Präfix belegt), nicht auf die Liste. (3) Tableau schlägt die Beziehung `Branch Id = Branch Id (Dim Branch)` selbst vor; Spalten mit Endung `Id` werden Dimensionen, `hour` und `item_count` Measures — der Stolperstein „SUM(Order Id)“ wurde deshalb zu „SUM(Hour)“. (4) CSV in deutscher Oberfläche: `v_filiale.csv` wird am Unterstrich getrennt (Feldtrennzeichen auf Komma), Dezimalpunkte ergeben Text (Gebietsschema Englisch (Vereinigte Staaten); Typumstellung auf Zahl liefert NULL), `monat` bleibt Text bis „Datentyp ändern → Datum“; die Checkliste W05-05 und die Befehlskarten B01, B02 und B04 sind auf den geprüften Weg umgeschrieben, die Quizfrage zur Meldung ebenfalls. (5) Die zwei Nachbildungen der Oberfläche sind durch sieben Bildschirmfotos unter `web/lab/assets/tableau/` ersetzt (588 KB, Ziffern im Bild, Legenden wie zuvor; `bm.css` erhält `.foto`-Regeln, die Begründung im Stylesheet nennt die Ausnahme), `verify.mjs` prüft Bilddateien und Alternativtexte (794 Zusicherungen, 0 Fehler). Nicht geprüft: die Public Edition selbst, `AVG([Stars])` (fact_reviews nicht geladen), der Wortlaut des Treiberhinweises, Preis und Lizenz.

**Stand 21. September 2026 (Prüfung von Lab 04 mit Power BI Desktop, dazu ein Dashboard):** Lab 04 wurde in Roberts Parallels-VM (Windows 11, deutsche Oberfläche) mit Power BI Desktop 2.157.1354.0 (August 2026) und dem Demo-Konto durchgespielt: Verbindung, Navigator, Import der sieben Tabellen, Modellansicht, fünf Measures, Seite „Bericht“ (Tabelle der Kontrollzahlen, Balken je Filiale, Linie je Monat, Seitenfilter 2025) — Kontrollzahlen 2.994.771,13 € · 136.557 · 21,93 € · 3,75 wie in `v_kennzahlen_jahr`. Auf Roberts Wunsch kam eine Seite „Dashboard“ nach den Regeln aus `references/diagramme.md` (Tufte, Bissantz, IBCS) dazu: Titel als Measure, vier Kacheln mit Abweichung zum Vorjahr, Matrix der Filialen mit Datenbalken hinter der Zahl, Ist gegen Vorjahr je Monat mit markiertem letztem Punkt, Abweichung als eigene Zeile in Grün/Rot, Small Multiples je Filiale in gleicher Skala, Monatskürzel auf den Achsen; 20 zusätzliche Measures und die Spalte `Monat` (Vorlage `web/lab/vorlagen/powerbi-dashboard.dax`), das Projekt als PBIP ohne Daten und Kennwort unter `powerbi/BurgerMetrics/` (die Seite „Dashboard“ ist als PBIR-JSON geschrieben und in Power BI geladen, geprüft und gespeichert). Befunde und Folgen im Lab: (1) Die Verbindung ist ein einziger Dialog „Mit Datenquelle verbinden“ (Server mit Port, Datenbank, Importieren/DirectQuery, Erweiterte Optionen mit SQL-Anweisung, Authentifizierungsart Standard, Datenschutzebene, Kästchen „Verschlüsselte Verbindung verwenden“); der Wortlaut einer Warnung zur unverschlüsselten Verbindung trat nicht auf (Anmeldung war gespeichert) und wird nicht mehr behauptet. (2) Der Navigator listet Tabellen und gewöhnliche Sichten (48 Einträge), keine materialisierten — die 38 `v_*` fehlen; Weg über „SQL-Anweisung“ mit Genehmigung der nativen Abfrage belegt (`v_kennzahlen_jahr`, zehn Zeilen). (3) Nach dem Laden entstanden acht Beziehungen (alle `_id`-Spalten, im Projekt `AutoDetected_`), die zwei Fremdschlüssel auf `dim_date.date` fehlten trotz beider Optionen und wurden von Hand angelegt; `fact_reviews → fact_orders` legte Power BI selbst als 1:1 inaktiv an (`order_id` beidseitig eindeutig). Die Aussage „13 Fremdschlüssel → 13 Beziehungen“ ist gestrichen. (4) DirectQuery über `fact_order_items` scheitert mit dem belegten Wortlaut „Das Resultset einer Abfrage an eine externe Datenquelle hat die maximal zulässige Größe von 1000000 Zeilen überschritten.“; die inaktive Filialbeziehung zeigt in jedem Balken die Gesamtsumme plus eine Zeile „(Leer)“ (nachgestellt). (5) Die zwei Nachbildungen sind durch 20 Bildschirmfotos unter `web/lab/assets/powerbi/` ersetzt (2,6 MB); Lab 04 hat einen neuen Abschnitt „Das Dashboard“, Befehlskarten B03/B04, Checkliste W04-05 mit acht Schritten, Quizfrage zu den Beziehungen; `LABS` in `bm.js` nennt 150 Minuten; `verify.mjs` 803 Zusicherungen, 0 Fehler. Werkzeuglehre: Parallels-Fenster nur per `computer_batch` bedienbar; Mac-seitiges Schreiben in den VM-Ordner hinterlässt `._*`-Dateien, die Power BI als TMDL zu lesen versucht — vor jedem Öffnen löschen. Nicht geprüft: der Wortlaut der Verschlüsselungswarnung, die Fan Trap im Modell (Zahlen aus SQL), das Verhalten mit allen 14 Tabellen.

**Stand 22. September 2026 (Kennzahlen-Dashboard in Tableau):** Auf Roberts Wunsch wurde das Dashboard aus Lab 04 mit Tableau Desktop 2026.2 (macOS, deutsche Oberfläche, Demo-Konto) nachgebaut und Lab 05 um den Abschnitt „Das Kennzahlen-Dashboard“ erweitert (Roberts Vorgabe vom 22.09.: keine Nennung von Tufte/Bissantz in Überschriften — auch die Lab-04-Einleitung angepasst): Datenquelle aus `fact_orders`, `dim_branch` und `fact_reviews` (Beziehung `Order Id = Order Id`, von Tableau nicht vorgeschlagen) als Extrakt; Parameter `Jahr` statt eines Filters, weil ein Filter das Vorjahr aus dem Blatt entfernen würde — Ist und Vorjahr als `SUM(IF YEAR([Date]) = [Jahr] THEN [Net Total] END)` und `[Jahr] - 1`, ohne Datumstabelle; 27 Calculated Fields (`web/lab/vorlagen/tableau-dashboard-felder.txt`, Befehlskarte B05), darunter der rote Punkt als Doppelachse mit einem Feld, das nur im jüngsten Monat einen Wert hat, `Top Filiale` als verschachtelter FIXED-Ausdruck und der Titel als Feld (`STR()` schreibt Dezimalpunkte, `REPLACE` das Komma); zehn Blätter; nach Roberts Rückmeldung zur ersten Fassung („zu gedrängt, zu wenig Weißraum, untere Grafik zu lang gestreckt“) neu gesetzt: 1600 × 1100, ein vertikaler Container als Rahmen mit horizontalen Containern fester Höhe je Reihe (64/140/390), zwölf Pixel äußere Füllung je Kachel und 20 am Rand, Trenn- und Rasterlinien über Format → Linien entfernt, die Small Multiples in der dritten Runde (Roberts Rückmeldung: Filialname als Überschrift, nicht als Label; gleiche Höhen; letzter Monat an der Achse; Dropdown) als zwei Blätter zu je vier Filialen mit `Branch Name` auf Spalten (Überschrift), Filter auf die Tabellenberechnung `Zeile = INT((INDEX() - 1) / 4)` (0/1, Berechnen per Branch Name), feste Achse 10–60, Teilstriche der Monatsachse alle zwei Monate ab Februar (Dez. angetragen); Balken und Tabelle auf „Gesamte Ansicht“, damit die mittlere Reihe unten bündig schließt; die Abweichung behält ihre Achse in Weiß plus 25 px innere Füllung, ihr Titel ist ein Textobjekt, damit Linie und Säulen monatsgenau übereinanderstehen; Linienachsen ohne Null, Monatskürzel über das Datumsformat „Abkürzung“; der Parameter mit Werteliste 2018–2026 unverankert (Layout x 1380, y 24, 180 × 56) als Kompaktliste (Dropdown), Blatttitel als Legende mit `<Parameter.Jahr>`, Achsentitel und Feldbeschriftungen ausgeblendet, Aliasse für die Spaltenköpfe (Befehlskarte B06). Kontrollzahlen wie in Lab 04: 2.994.771 · 136.557 · 21,93 · 3,750, Abweichungen +5,4 % · +2,4 % · +2,9 % · +0,05 Sterne, Titel „Umsatz 2025: 2,99 Mio. €, +5,4 % zum Vorjahr – BM Europastern liegt vorn“. Zwölf weitere Bildschirmfotos unter `web/lab/assets/tableau/` (Fensteraufnahmen; Menüs und Dialoge als eigene Fenster aufgenommen und an ihrer Bildschirmposition eingesetzt, weil eine Vollbildaufnahme fremde Fenster mitnähme), die Arbeitsmappe unter `tableau/BurgerMetrics.twb` (XML, ohne Daten und Kennwort, `tableau/README.md`), Sperrdatei über `.gitignore` ausgeschlossen; Quiz W05-03 um die Frage Parameter gegen Filter, Checkliste W05-05 um die Kür ergänzt; Lab 05 auf 150 Minuten; `verify.mjs` 815 Zusicherungen, 0 Fehler. Befunde: (1) Tableau-Kacheln im Raster lassen sich nicht über die Größenfelder des Layout-Reiters setzen (nur Anzeige), Ränder werden gezogen oder Container verteilt; (2) Anpassung und Blatttitel sind je Kachel im Dashboard einzustellen, nicht im Blatt; (3) ein Textblatt in Standard-Anpassung bricht Spaltenköpfe um, im Dashboard erst nach einer höheren Kopfzeile — deshalb Aliasse mit Trennstrich („Bestel- lungen“); (4) `STR()` einer Zahl liefert den Dezimalpunkt der Formelsprache, nicht der Oberfläche; (5) Kacheln bekommen ihre Höhe verlässlich nur über „Höhe fixieren“ im Menü des umgebenden Containers, die Größenfelder im Layout-Reiter sind bei Rasterkacheln nur Anzeige; (6) eine Tabellenberechnung mit `INDEX()` zählt ohne „Berechnen per“ die innerste Dimension (Monate) — für das Raster muss sie über `Branch Name` laufen. Vierte Runde (Robert): alle Hilfslinien entfernt — je Blatt Format → Linien (Raster-, Null-, Achsenmaßstäbe, Achsenteilstriche „Keine“, Reiter Blatt/Zeilen/Spalten getrennt) und Format → Rahmen (Zeilen-/Spaltentrennung „Keine“); Lab 05 verlinkt die `.twb` im Repository (Vorlagenkarte, Stand-Hinweis, Fotohinweis). Fünfte Runde (Robert, nach dem Merge): Signalfarben plus Blau `#4E79A7` (Blau der Ist-Linie) und minus Rot `#E15759` statt Grün — in der Mappe war minus bis dahin das Tableau-Standardblau; gesetzt über „Farben bearbeiten…“ am Feld `Δ Umsatz Richtung` (der Wert „minus“ erscheint dort erst, wenn der Parameter auf ein Jahr mit Minus-Monaten steht) und als Schriftfarbe der Δ-Zeile in den vier Karten; neues Bild `dashboard-farben.png`, Lab-Text nennt die Grenze, dass Kartentexte eine feste Schriftfarbe haben. Lab 04 (Power BI) bleibt bei Grün, bis Robert entscheidet. Weitere Befunde: (7) ein Blatt ohne Achse hat einen breiteren Zeichenbereich als eines mit Achse — gleiche Monatsspalten nur mit unsichtbarer (weißer) Achse; (8) eine Kachel im Raster bekommt Weißraum über „Äußere Füllung“, ihre Höhe über den umgebenden Container; (9) INDEX() für Small Multiples zählt ohne „Berechnen per“ die Monate. Nicht geprüft: das Öffnen der `.twb` auf einem anderen Rechner (fehlender Extrakt) und Tableau Public.

## 12 Reihenfolge

1. Spec committen (erledigt in dieser Sitzung).
2. Datenbank und Rezensionen: `0021`, Generator, Glättung, CSV laden, ETL, Mini-Skripte, Doku.
3. Shop-Feature.
4. Notebooks und Dash (Notebook 08 braucht Phase 2).
5. Deck (braucht Bildschirmfotos aus 3 und 4).
6. BM-Lab unter `web/lab/` (verweist auf 3, 4 und das Deck).
7. Verknüpfungen, Dokumentation, letzter PR; der Merge veröffentlicht Startseite, Shop und Lab zusammen.

Je Phase ein eigener Implementierungsplan mit `writing-plans`; Phasen 3 bis 5 lassen sich
parallelisieren, sobald Phase 2 steht.

## 13 Risiken und offene Punkte

* **LFS-Kontingent.** GitHub gibt 1 GB Bandbreite je Monat für LFS; ein Kurs, der die großen
  CSV-Dateien aus Colab lädt, überschreitet das. Deshalb lesen die Notebooks große Tabellen aus
  der Datenbank; `lade_csv` steht nur für Dimensionen. Auf den Folien wird das benannt.
* **Erreichbarkeit von Port 5433 aus Colab** ist plausibel (der Port ist öffentlich), aber
  in der Umsetzung zu Beginn von Phase 4 einmal zu prüfen.
* **OpenLigaDB** deckt die Regionalliga Bayern möglicherweise nicht ab; dann eine kleine, von
  Hand gepflegte CSV der Heimspieltage mit Quellenangabe.
* **Glättungsvolumen.** 10.000 Texte in 100 Losen sind viel Ausgabe; das Abbruchkriterium in
  4.2 hält den Bestand konsistent, auch wenn nicht alle Lose geglättet werden.
* **Synthetischer Bestand.** Effekte externer Daten sind nicht real; jedes Notebook, das externe
  Daten nutzt, sagt das im Abschnitt „Was offen bleibt“.
* **Tableau und Nur-Lese-Transaktionen.** `studi_daba` hat `default_transaction_read_only = on`;
  Tableau legt für manche Filter und Extrakte temporäre Tabellen an, was dann scheitert. Am
  19.09.2026 mit Tableau Desktop 2026.2 durchgespielt: Tableau prüft die Fähigkeit beim Verbinden
  (`SELECT * INTO TEMPORARY TABLE … Connect_Check`, danach `CREATE LOCAL TEMPORARY TABLE …`),
  bekommt SQLSTATE 25006 `cannot execute SELECT INTO in a read-only transaction` bzw. `cannot
  execute CREATE TABLE in a read-only transaction`, zeigt keine Meldung und arbeitet mit
  Unterabfragen weiter; Kontextfilter, Jahresfilter und der Extract von `fact_orders` (754.513
  Zeilen) liefen ohne Fehler. Lab 05 nennt den Vorgang mit den Meldungen als Stolperstein; die
  zweite Schranke (`permission denied to create temporary tables`) erreicht Tableau nicht.
* **Öffentlicher Schreibweg.** Wie bei Bestellungen: Längen- und Ratenbegrenzung, keine
  Anzeige von Besuchertexten, Rücksetzfunktion. Ein Missbrauch verändert keine Kennzahl des
  kuratierten Bestands, weil der ETL bewusst aufgerufen wird.
